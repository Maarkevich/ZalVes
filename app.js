const KEY='zalves-data-v1';const today=new Date().toISOString().slice(0,10);let data=JSON.parse(localStorage.getItem(KEY)||'{"workouts":[],"measurements":[],"profile":{"name":"","goal":"Набор мышц"}}');const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];function save(){localStorage.setItem(KEY,JSON.stringify(data));render()}function fmtDate(d){return new Date(d+'T12:00:00').toLocaleDateString('ru-RU',{day:'numeric',month:'short'})}function esc(s=''){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}function openModal(id){const m=$('#'+id);m.classList.add('open');const f=m.querySelector('form');f.date.value=today}function closeModals(){$$('.modal-backdrop').forEach(x=>x.classList.remove('open'))}function navigate(id){$$('.view').forEach(v=>v.classList.toggle('active',v.id===id));$$('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.view===id));window.scrollTo(0,0)}function lineChart(values,color='var(--accent)'){if(!values.length)return '<div class="empty">Недостаточно данных для графика</div>';const w=500,h=165,min=Math.min(...values),max=Math.max(...values),range=max-min||1;const pts=values.map((v,i)=>`${(i/(values.length-1||1))*w},${h-15-((v-min)/range)*(h-30)}`).join(' ');return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><polyline points="${pts}" fill="none" stroke="${color}" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>${values.map((v,i)=>`<circle cx="${(i/(values.length-1||1))*w}" cy="${h-15-((v-min)/range)*(h-30)}" r="4" fill="${color}"/>`).join('')}</svg>`}function render(){const ms=[...data.measurements].sort((a,b)=>a.date.localeCompare(b.date));const ws=[...data.workouts].sort((a,b)=>b.date.localeCompare(a.date));const last=ms.at(-1);$('#dashWeight').textContent=last?.weight||'—';$('#dashChest').textContent=last?.chest||'—';$('#weightSpark').innerHTML=ms.length>1?lineChart(ms.map(x=>x.weight).filter(Boolean)):' ';$('#chestSpark').innerHTML=ms.length>1?lineChart(ms.map(x=>x.chest).filter(Boolean),'var(--purple)'):' ';$('#dashWorkout').textContent=ws[0]?.title||'Пока нет записей';$('#dashWorkoutMeta').textContent=ws[0]?`${fmtDate(ws[0].date)} · ${ws[0].exercise} · ${ws[0].weight} кг`:'Создайте тренировку в разделе «Зал»';$('#streakText').textContent=data.profile.name?`${data.profile.name}, ${data.profile.goal.toLowerCase()}`:'Добавьте первую запись';$('#workoutList').innerHTML=ws.length?ws.map(w=>`<article class="record"><div class="record-top"><div><strong>${esc(w.title)}</strong><small>${fmtDate(w.date)} · ${esc(w.exercise)}</small></div><span class="metric">${w.weight} кг</span></div><p>${w.sets} подхода × ${w.reps} повторений${w.note?' · '+esc(w.note):''}</p></article>`).join(''):'<div class="empty">Тренировок пока нет.<br>Нажмите «＋», чтобы добавить первую.</div>' ;$('#measurementList').innerHTML=ms.slice().reverse().map(m=>`<article class="record"><div class="record-top"><div><strong>${fmtDate(m.date)}</strong><small>Замер тела</small></div><span class="metric">${m.weight?m.weight+' кг':''}</span></div><p>${[['Грудь',m.chest],['Талия',m.waist],['Бицепс',m.arm]].filter(x=>x[1]).map(x=>x[0]+': '+x[1]+' см').join(' · ')||'Только заметка'}${m.note?' · '+esc(m.note):''}</p></article>`).join('')||'<div class="empty">Добавьте первый замер тела.</div>';renderChart();renderWeek();$('#profileName').value=data.profile.name;$('#profileGoal').value=data.profile.goal}function renderChart(type='weight'){const ms=[...data.measurements].sort((a,b)=>a.date.localeCompare(b.date));const vals=type==='body'?ms.map(x=>x.chest).filter(Boolean):type==='lifts'?[...data.workouts].sort((a,b)=>a.date.localeCompare(b.date)).map(x=>x.weight):ms.map(x=>x.weight).filter(Boolean);$('#chartLabel').textContent=type==='body'?'Объём груди, см':type==='lifts'?'Рабочий вес, кг':'Вес тела, кг';$('#chartValue').textContent=vals.at(-1)?vals.at(-1):'—';$('#chartTrend').textContent=vals.length>1?(vals.at(-1)-vals[0]>=0?'↗ Рост':'↘ Снижение'):'Нет данных';$('#largeChart').innerHTML=lineChart(vals,type==='body'?'var(--purple)':'var(--accent)')}function renderWeek(){const days=['Пн','Вт','Ср','Чт','Пт','Сб','Вс'];const now=new Date();const start=new Date(now);start.setDate(now.getDate()-((now.getDay()+6)%7));$('#weekChart').innerHTML=days.map((d,i)=>{const date=new Date(start);date.setDate(start.getDate()+i);const key=date.toISOString().slice(0,10),done=data.workouts.some(x=>x.date===key);return `<div class="day-bar"><div class="bar ${done?'done':''}" style="--h:${done?'62px':'22px'}"></div>${d}</div>`}).join('')}$$('[data-view]').forEach(b=>b.onclick=()=>navigate(b.dataset.view));$$('[data-modal]').forEach(b=>b.onclick=()=>openModal(b.dataset.modal));$$('.close-modal').forEach(b=>b.onclick=closeModals);$$('.modal-backdrop').forEach(m=>m.onclick=e=>{if(e.target===m)closeModals()});$$('.tab').forEach(b=>b.onclick=()=>{$$('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderChart(b.dataset.chart)});$('#workoutModal form').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);data.workouts.push(Object.fromEntries(f));save();closeModals();e.target.reset();toast('Тренировка сохранена')};$('#measurementModal form').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target);const item=Object.fromEntries(f);Object.keys(item).forEach(k=>{if(item[k]==='')delete item[k]});data.measurements.push(item);save();closeModals();e.target.reset();toast('Замер сохранён')};$('#saveProfile').onclick=()=>{data.profile={name:$('#profileName').value.trim(),goal:$('#profileGoal').value};save();toast('Профиль сохранён')};$('#exportData').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='zalves-backup.json';a.click();toast('Файл подготовлен')};$('#clearData').onclick=()=>{if(confirm('Удалить все тренировки и замеры?')){data={workouts:[],measurements:[],profile:data.profile};save();toast('Данные удалены')}};let deferred;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferred=e;$('#installBtn').hidden=false});$('#installBtn').onclick=async()=>{if(deferred){deferred.prompt();deferred=null}};if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');render();